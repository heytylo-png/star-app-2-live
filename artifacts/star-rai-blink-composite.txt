Star Rai — idle blink composite (no full-PNG clip)

Problem
Blink flash = two full PNGs clipping / crossfading over each other.
Stop that. Body must not punch.

Composite only (authoritative steps)
1. Register blink sheet onto the LIVE idle canvas (public/rai/idle.png, 1008×1792).
   Scale/letterbox first if the blink source is 720×1280 (or any other size).
2. Cut the same two eye holes from glare (live idle) and from the registered blink
   (same x,y,w,h per eye for both sheets).
3. Paste blink holes into the glare body. Produce 2–4 frames. Restore glare eyes
   after the closed-lid hold (back to open glare eyes).
4. Do not cut hair, ahoge, mouth, or collar. Stay on idle key only.

Runtime
- Do not opacity-crossfade / stack two full-body PNGs for blink.
- Animate using the composite frames (glare body + eye holes only), or an
  equivalent eye-region composite on the live idle key.
- Rest/idle only; cancel mid-blink on pose/talk/tint; prefers-reduced-motion off.
- Named poses do not blink.

Assets
- Live glare/idle key: public/rai/idle.png (1008×1792) — never replace this as default.
- Blink source: public/rai/idle_blink.png (or official closed-lid still registered to that canvas).
- Ship baked composites (and/or replace idle_blink.png with a body-locked composite)
  so outside the two eye holes the pixels match live idle (max delta 0).

Done when
Lids close on rest idle and the body does not punch / flash.

Out of scope
Spine flag, 3D, generated lids, Expo face_eyes_* busts, Chat tint/tabs/copy, Pages deploy.

Eye holes (1008×1792 idle canvas — v4 soft ellipses)
The earlier rects (L 434,208 80×28; R 514,208 98×30) are wrong: the right hole ate the ear and the iris leaked. Hold these only.
Bangs above y=202, ahoge, mouth, and collar are outside these rects.
left:  x=432 y=202 w=80 h=40
right: x=508 y=202 w=80 h=40
Ovals: L cx=472 cy=222 rx=38 ry=18 · R cx=548 cy=222 rx=38 ry=18
Constants: IDLE_BLINK_EYE_HOLES in src/lib/rai.ts.
Source of truth: artifacts/star-rai-blink-frames/eyes/ (RGBA hole crops + proof strip).
scripts/bake-idle-blink.py still knows the rects, but re-running it overwrites the v4 ellipses with an opaque mix. Do not run it over those crops.

Frames
Seven steps, about two frames each (24fps twos, IDLE_BLINK_STEP_MS). L/R holes only.
1 01-open        artifacts/star-rai-blink-frames/eyes/01-open_L.png + _R.png
2 02-closing     02-closing_L.png + _R.png
3 03-half        03-half_L.png + _R.png
4 04-462-blink   04-462-blink_L.png + _R.png
5 03-half
6 02-closing
7 01-open
The raw 782/783 sheets are not body-locked: outside the eye holes their delta
vs idle.png is 255. Never mount those JPGs as idle or as a full-body blink plate.
Hole-only composites must match idle.png outside the holes (max delta 0).
01-open matches the idle eye holes after source-over (inside delta 0). It is still the open crop, not a second body.
Runtime copies of the pack (byte-identical):
- public/rai/idle_blink_open_l.png / idle_blink_open_r.png — 01-open
- public/rai/idle_blink_01_l.png / idle_blink_01_r.png — 02-closing
- public/rai/idle_blink_02_l.png / idle_blink_02_r.png — 03-half
- public/rai/idle_blink_l.png / idle_blink_r.png — 04-462-blink
Full plates idle_blink.png / idle_blink_01.png / idle_blink_02.png stay unmounted.

Runtime
- One body texture the whole blink: live public/rai/idle.png, never unloaded.
- The idle canvas is 1008×1792. On mount, and when idle.png is ready, draw that full bitmap once. Do not leave the canvas at the browser default 300×150.
- Blink only drawImages the two eye rects (L 432,202 80×40; R 508,202 80×40). Each lid step restores the glare rect from idle.png, then source-over draws the soft ellipse so transparent corners do not punch the body.
- No second <img>. No opacity crossfade of two full figures. No canvas clear between blink frames.
- Each lid step holds about two frames. Close and open are steps, not an open↔closed pop.
- If those crops are not ready, blink stays off. Do not swap in a full plate or a 782/783 sheet.
- Proof: pixels outside the two rects stay byte-identical to the idle bitmap through close and open.
