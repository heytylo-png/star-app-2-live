# Rive (track #3) — stub

Not this PR. After a Spine / cutout rig is proven on official art, Rive can own the interactive pass (state machine, pointer, Call amplitude → mouth).

- Do not add `@rive-app/react-canvas` until that track.
- Do not buy Live2D. Do not use After Effects. No AI video as the live body.
- Official PNGs stay the shipping face until an engine is proven.

When this track starts: drop a `.riv` here, load it behind a flag (`?rive=1`), keep PNG default, keep `?spine=*` as track #2.
